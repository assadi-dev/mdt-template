import styled from "styled-components";

export const HeaderTabSelect = styled.div`
  border-radius: 5px;
  width: 100%;
  padding: 1rem;
  display: flex;
  flex-wrap: wrap;
  margin-bottom: 1.5rem;
  background-color: rgba(0, 0, 0, 0.6);
  justify-content: space-evenly;
  grid-gap: 1rem;

  .header-tab-btn {
    background-color: transparent;
    padding: 1rem 1.3rem;
    margin: 0 0.3rem;
    border-radius: 5px;
    cursor: pointer;
    transition: all 0.35s;
  }
`;
